"""A collection of monolythic objects for phpsploit framework.

This module contains a list of objects used by the framework's core.
Each one if directly available from there.

Example:
    >>> from objects import MetaDict
    >>> dic = MetaDict()

"""

from . MetaDict       import MetaDict
from . VarContainer   import VarContainer

from . import buffers
